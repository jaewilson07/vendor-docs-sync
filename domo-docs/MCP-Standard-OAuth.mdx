---
title: "Connect to the Domo Essentials MCP Server with Standard OAuth"
excerpt: "How to connect to the Domo Essentials MCP Server with standard OAuth."
tag: "Beta"
---

import { BetaNote } from '/snippets/BetaNote.mdx';

<BetaNote />

## Intro

This article explains how to connect to the Domo Essentials MCP Server using standard OAuth.

---

## Prerequisites

<Note>
  - You need **AI Chat** or **AI Library** enabled to use MCP.
  - You also need the **Use MCP** grant enabled.
</Note>

Visit the **AI Library** and retrieve the MCP server URL from the **Domo Basic MCP** toolkit.

```
https://<DOMO_DOMAIN>/api/ai/v1/assistant/mcp/toolkits/domo_basic_mcp
```

<Frame><img alt="AI Library showing the Domo Basic MCP toolkit and server URL" src="/images/kb/domo_basic.png" /></Frame>

## Add a Custom Connector

In Claude Cowork or Chat, customization lets you tailor your AI agent using the **Customize** menu on the left sidebar of the Claude Desktop app.

<Frame><img alt="Claude Desktop left sidebar with Customize menu" src="/images/kb/claude_3.png" /></Frame>

Link third-party applications like remote MCPs by selecting **Customize** > **Connectors** to give Claude access to your data.

<Frame><img alt="Claude Desktop Connectors panel under Customize" src="/images/kb/claude_2.png" /></Frame>

Give the MCP server a name and paste the **Domo Basic MCP** URL into the **Remote MCP server URL** field, then select **Add**.

```
https://<DOMO_DOMAIN>/api/ai/v1/assistant/mcp/toolkits/domo_basic_mcp
```

<Note>**Note:** Replace `<DOMO_DOMAIN>` with your actual Domo instance domain (for example, `mycompany.domo.com`).</Note>

<Frame><img alt="Claude Desktop Add Connector dialog with Remote MCP server URL field" src="/images/kb/claude_1.png" /></Frame>

## Authenticate with OAuth

Select **Connect** and follow the prompts to log into your Domo instance.

<Frame><img alt="Claude Desktop Connect prompt for Domo MCP server" src="/images/kb/claude_4.png" /></Frame>

<Frame><img alt="Domo OAuth login, step one" src="/images/kb/login_1.png" /></Frame>

<Frame><img alt="Domo OAuth login, step two" src="/images/kb/login_2.png" /></Frame>

<Frame><img alt="Domo OAuth login, step three" src="/images/kb/login_3.png" /></Frame>

After authentication is complete, the connector is active and Claude has access to your Domo instance.

<Frame><img alt="Claude Desktop showing active Domo MCP connection" src="/images/kb/complete.png" /></Frame>

## OAuth FAQ

<AccordionGroup>

<Accordion title="What if I don't know my Domo domain?">
  Your Domo domain is the subdomain in the URL you use to log into Domo — for example, if you access Domo at `mycompany.domo.com`, your domain is `mycompany.domo.com`. Contact your Domo admin if you are unsure.
</Accordion>

<Accordion title="Can I connect multiple Domo instances?">
  Yes. Repeat the steps in this article for each instance, using a unique connector name and the corresponding domain in the MCP server URL for each.
</Accordion>

<Accordion title="What data does Claude have access to once connected?">
  Claude can access the data and tools exposed by the Domo Basic MCP toolkit, which includes querying DataSets and interacting with Domo features available through the MCP API. Access is scoped to what your Domo user account is permitted to see.
</Accordion>

</AccordionGroup>
